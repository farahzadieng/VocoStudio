# VOCO APP

Unzip `Model-Downloader.zip` and run the python app to download latest models into `./checkpoints'

Install requirements :

```bash
uv sync
```

Run App :

```bash
uv run python run_webapp.py
```
